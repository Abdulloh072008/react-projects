import React from 'react'

const Section2btn = ({name}) => {
    return (
        <div>
            <button className='btn bg-[white] rounded-[20px] text-[black] w-full' style={{ padding: "7px 20px"}}>{name}</button>
        </div>
    )
}

export default Section2btn
